# HTTPS Properties
https:
  host:           "0.0.0.0"
  port:           "8082"

# SSL/TLS Properties 
tls.keystore:
  type:           "pkcs12"
  path:           "check-in-papi.p12"
  alias:          "server"
  password:       "mule12345"
  keyPassword:    "mule12345"

# check-in-papi API Spec Properties 
api:
  groupId:        "${api.groupId}"
  artifactId:     "${api.artifactId}"
  version:        "${api.version}"
  spec:           "resource::${api.groupId}:${api.artifactId}:${api.version}:oas:zip:api.json"
  majorVersion:   "v1"
  id:             "17467821"